{
  "_comment": "Files delivered into the Chromium checkout by copying rather than patching. Sources are relative to this repo, dests relative to src/. Applied by scripts/apply_patches.py after the patch set, removed by scripts/reset_patches.py. Use 'copies' for one-off binary assets (icons), 'trees' for whole new source directories that mirror the src/ layout.",
  "copies": [],
  "trees": [
    {
      "source": "overlay",
      "dest": ".",
      "exclude": ["**/*.md"]
    }
  ]
}
